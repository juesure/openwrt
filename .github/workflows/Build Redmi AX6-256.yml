# 工作流名称
name: Build Redmi AX6 (256M)

# 触发条件：手动触发（推荐）+ 代码提交触发
on:
  push:
    branches: [ juesure-patch-1 ]
  workflow_dispatch: # 核心：支持手动点击触发编译

# 全局环境变量
env:
  # 源码配置（Lean大 LEDE 源码，适配 IPQ807x）
  REPO_URL: https://github.com/juesure/openwrt/
  REPO_BRANCH: juesure-patch-1
  # 自定义脚本路径（匹配你实际存放的位置：openwrt/scripts/diy-ax6-256m.sh）
  DIY_SCRIPT: /home/runner/work/openwrt/openwrt/scripts/diy-ax6-256m.sh
  # 编译配置文件（提前生成的 Redmi AX6 配置）
  CONFIG_FILE: /home/runner/work/openwrt/openwrt/config/ax6-256m.config
  # 时区
  TZ: Asia/Shanghai
  # 定义工作目录（固定路径）
  WORK_DIR: /home/runner/work/openwrt/openwrt/workdir
  # 固件输出目录（核心修正：高通IPQ807x正确路径）
  FIRMWARE_OUTPUT_DIR: /home/runner/work/openwrt/openwrt/openwrt/bin/targets/qualcommax/ipq807x

jobs:
  build-ax6-firmware:
    runs-on: ubuntu-22.04
    permissions:
      contents: write # 赋予 Release 发布权限

    steps:
      # 步骤1：拉取仓库文件（包含 scripts 目录下的 diy 脚本）
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          path: /home/runner/work/openwrt/openwrt # 强制拉取到固定路径，确保脚本位置正确

      # 步骤2：初始化编译环境（安装依赖+清理空间，修复权限）
      - name: Initialize Build Env
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          # 清理冗余文件释放空间
          sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc
          sudo apt update -y && sudo apt full-upgrade -y
          # 安装 OpenWrt 编译依赖（含 mtd-utils 用于生成编程器固件）
          sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib \
          gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev file wget \
          libelf-dev libfuse-dev device-tree-compiler mtd-utils squashfs-tools dosfstools parted kmod
          sudo timedatectl set-timezone "$TZ"
          # 核心修复：创建固定路径的工作目录（避免变量解析错误）
          mkdir -p $WORK_DIR
          # 验证目录权限（调试用）
          ls -ld $WORK_DIR
          # 验证自定义脚本是否存在（关键：提前检查脚本路径）
          if [ -f $DIY_SCRIPT ]; then
              echo "✅ 自定义脚本找到：$DIY_SCRIPT"
              chmod +x $DIY_SCRIPT # 赋予执行权限
          else
              echo "❌ 自定义脚本不存在：$DIY_SCRIPT"
              ls -l /home/runner/work/openwrt/openwrt/scripts/ # 列出目录文件，便于排查
              exit 1
          fi

      # 步骤3：克隆 OpenWrt 源码（使用固定可写目录）
      - name: Clone OpenWrt Source
        run: |
          # 切换到工作目录执行
          cd $WORK_DIR
          # 查看磁盘空间使用情况（调试用）
          df -hT $PWD
          # 克隆源码（指定仓库地址和分支）
          git clone --depth=1 $REPO_URL -b $REPO_BRANCH openwrt
          # 创建软链接，方便后续脚本访问源码目录
          ln -sf $WORK_DIR/openwrt /home/runner/work/openwrt/openwrt/openwrt
          # 确保源码目录可写
          chmod -R 755 $WORK_DIR/openwrt

      # 步骤4：注入 256M 扩容修改（核心：使用正确的脚本路径）
      - name: Inject 256M Patch
        run: |
          cd /home/runner/work/openwrt/openwrt/openwrt
          # 执行扩容修改脚本（使用实际路径）
          $DIY_SCRIPT

      # 步骤5：更新/安装 Feeds（独立步骤，语法正确）
      - name: Update & Install Feeds
        run: |
          cd /home/runner/work/openwrt/openwrt/openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a  # 这行是独立的命令，单独放在 run 块中

      # 新增：Clean mt76 Driver（Critical Fix）→ 独立步骤，层级正确
      - name: Clean mt76 Driver (Critical Fix)
        run: |
          cd /home/runner/work/openwrt/openwrt/openwrt
          # 彻底删除 mt76 源码和编译缓存
          rm -rf package/kernel/mt76
          rm -rf build_dir/target-*/linux-mediatek*
          rm -rf staging_dir/target-*/linux-mediatek*
          # 重新生成配置，确保 mt76 被禁用
          make defconfig

      # 步骤6：加载 AX6 编译配置
      - name: Load AX6 Config
        run: |
          # 复制预生成的 AX6 配置文件（使用实际路径）
          if [ -e $CONFIG_FILE ]; then
              cp $CONFIG_FILE /home/runner/work/openwrt/openwrt/openwrt/.config
              echo "✅ 编译配置文件加载成功：$CONFIG_FILE"
          else
              echo "❌ 编译配置文件不存在：$CONFIG_FILE"
              exit 1
          fi
          cd /home/runner/work/openwrt/openwrt/openwrt
          # 生成最终配置
          make defconfig

      # 步骤7：下载依赖包（加速编译）
      - name: Download Packages
        run: |
          cd /home/runner/work/openwrt/openwrt/openwrt
          make download -j$(nproc)
          # 清理空文件避免编译报错
          find dl -size -1024c -exec rm -f {} \;

      # 修复联发科残留文件
      - name: Clean Mediatek Files (Critical Fix)
        run: |
          cd /home/runner/work/openwrt/openwrt/openwrt
          # 删除联发科 EEPROM 相关依赖
          rm -rf package/firmware/mediatek/*mt7981*
          rm -rf package/firmware/mediatek/*mt7976*
          # 清理联发科平台编译配置
          sed -i '/mt7981_eeprom/d' target/linux/mediatek/image/Makefile 2>/dev/null || true
          # 彻底删除联发科平台目录（核心：避免prereq检查报错）
          rm -rf target/linux/mediatek 2>/dev/null || true
          # 修复Python数值转换脚本（解决0x20000000转int失败）
          sed -i 's/return int(image_size)/return int(image_size, 0)/' scripts/json_add_image_info.py
          # 禁用fwupd解决递归依赖
          echo -e "CONFIG_PACKAGE_fwupd=n\nCONFIG_PACKAGE_fwupd-libs=n\nCONFIG_FWUPD_PLUGIN_FLASHROM=n" >> .config
          # 重新生成配置
          make defconfig -e

      # 步骤8：编译固件（核心）
      - name: Compile Firmware
        id: compile
        run: |
          cd /home/runner/work/openwrt/openwrt/openwrt
          # 核心优化：先编译工具链，避免flock报错
          make tools/compile -j$(nproc) V=s
          # 多线程编译，失败则单线程排错（保留完整日志）
          make -j$(nproc) 2>&1 | tee compile_full.log || (echo "❌ 多线程编译失败，单线程重试..." && make -j1 V=s 2>&1 | tee compile_single.log)
          # 输出关键错误信息
          echo "🔍 编译错误日志片段："
          grep -E "ERROR|failed|ValueError|recursive dependency" compile_full.log compile_single.log || echo "✅ 无明显编译错误"
          # 检查固件是否生成
          if [ -d "$FIRMWARE_OUTPUT_DIR" ] && ls "$FIRMWARE_OUTPUT_DIR"/*redmi_ax6* 2>/dev/null; then
              echo "status=success" >> $GITHUB_OUTPUT
              echo "BUILD_TIME=$(date +%Y%m%d_%H%M)" >> $GITHUB_ENV
              # 提取设备型号
              grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
              echo "DEVICE_NAME=$(cat DEVICE_NAME)" >> $GITHUB_ENV
          else
              echo "❌ 固件未生成，编译失败！"
              exit 1
          fi

      # 步骤9：生成编程器固件（完整 Flash 镜像）
      - name: Generate Programmer Firmware
        if: steps.compile.outputs.status == 'success'
        run: |
          # 进入正确的固件输出目录（核心修正：替代target/linux/generic）
          cd $FIRMWARE_OUTPUT_DIR
          # 1. 找到Redmi AX6固件文件（精准匹配）
          FACTORY_BIN=$(ls *redmi_ax6*factory.bin 2>/dev/null)
          SYSUPGRADE_BIN=$(ls *redmi_ax6*sysupgrade.bin 2>/dev/null)
          
          if [ -z "$FACTORY_BIN" ]; then
              echo "❌ 未找到Redmi AX6 factory.bin固件！"
              ls -l $FIRMWARE_OUTPUT_DIR # 列出所有文件便于排查
              exit 1
          fi
          
          # 2. 生成 256M 编程器镜像（十进制268435456字节=256MB）
          dd if=/dev/zero of=ax6-256m-full.img bs=1 count=268435456
          # 3. 写入固件（适配IPQ807x NAND布局，偏移量按需调整）
          dd if=$FACTORY_BIN of=ax6-256m-full.img bs=1 seek=$((0x0FB40000)) conv=notrunc
          # 4. 打包编程器固件（方便刷写）
          zip -r ax6-256m-programmer-${{ env.BUILD_TIME }}.zip ax6-256m-full.img
          # 5. 标记固件目录
          echo "FIRMWARE_DIR=$(pwd)" >> $GITHUB_ENV

      # 步骤10：整理分区刷写固件
      - name: Organize Partition Firmware
        if: steps.compile.outputs.status == 'success'
        run: |
          cd ${{ env.FIRMWARE_DIR }}
          # 保留 factory/sysupgrade 固件（分区刷写用）
          mkdir -p partition-firmware
          cp *redmi_ax6*factory.bin *redmi_ax6*sysupgrade.bin partition-firmware/
          # 打包分区固件
          zip -r ax6-256m-partition-${{ env.BUILD_TIME }}.zip partition-firmware/

      # 步骤11：上传固件到 GitHub Artifact（临时下载）
      - name: Upload Firmware to Artifact
        uses: actions/upload-artifact@v4
        if: steps.compile.outputs.status == 'success'
        with:
          name: AX6-256M-Firmware-${{ env.BUILD_TIME }}
          path: |
            ${{ env.FIRMWARE_DIR }}/ax6-256m-programmer-${{ env.BUILD_TIME }}.zip
            ${{ env.FIRMWARE_DIR }}/ax6-256m-partition-${{ env.BUILD_TIME }}.zip
            ${{ env.FIRMWARE_OUTPUT_DIR }}/*redmi_ax6*.bin # 额外上传原始固件

      # 步骤12：发布固件到 GitHub Release（长期保存）
      - name: Release Firmware
        uses: softprops/action-gh-release@v2
        if: steps.compile.outputs.status == 'success'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: AX6-256M-${{ env.BUILD_TIME }}
          release_name: Redmi AX6 256M 固件-${{ env.BUILD_TIME }}
          body: |
            📦 固件类型：
            - ax6-256m-programmer.zip：编程器刷写固件（完整 256M Flash 镜像）
            - ax6-256m-partition.zip：分区刷写固件（factory/sysupgrade）
            - 原始固件：openwrt-qualcommax-ipq807x-redmi_ax6-*.bin
            📝 适配：256M Flash 扩容 + 避开坏块 + 修复所有编译报错
          files: |
            ${{ env.FIRMWARE_DIR }}/ax6-256m-programmer-${{ env.BUILD_TIME }}.zip
            ${{ env.FIRMWARE_DIR }}/ax6-256m-partition-${{ env.BUILD_TIME }}.zip
            ${{ env.FIRMWARE_OUTPUT_DIR }}/*redmi_ax6*.bin
